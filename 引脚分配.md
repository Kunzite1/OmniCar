# 引脚分配表

记录 STM32F407VET6 与各外设/模块的引脚接线。随硬件接线推进持续更新；凡是 CubeMX 已配置的引脚，以此表 + `OmniCar.ioc` 为准。

## 已接线

| 模块 | MCU 引脚 | MCU 信号 | 对接信号 | 说明 |
|---|---|---|---|---|
| 板载 LED | PA1 | `boardLED`（开漏输出） | 板载 | 低电平点亮，CubeMX 已配置，见 `Core/Inc/main.h` |
| 日志打印串口（USB 转 TTL） | PA2 | `USART2_TX` | USB 转 TTL 模块 RX | 波特率 **115200** 8N1，无流控 |
| 日志打印串口（USB 转 TTL） | PA3 | `USART2_RX` | USB 转 TTL 模块 TX | 与电脑共地（GND 相连） |

接线约定：**模块 TX → 单片机 RX，模块 RX → 单片机 TX**。

## 待接线（规划已定，接好线后核验）

以下引脚分配为当前规划（2026-08-15 定稿），接线完成后在此表基础上核对实际接法与方向极性。

| 模块 | 功能 | MCU 引脚 | 复用/资源 | 说明 |
|---|---|---|---|---|
| 电机 1（520 编码电机） | PWM | **PA6** | TIM3_CH1 | 调速 PWM（TIM3 共用） |
|  | 编码器 A / B | **PE9 / PE11** | TIM1_CH1 / CH2 | 高级定时器 TIM1，编码器模式 |
|  | 方向控制 A / B | **PE13 / PE14** | GPIO Output | 电机 1 方向 |
| 电机 2（520 编码电机） | PWM | **PA7** | TIM3_CH2 | 调速 PWM（TIM3 共用） |
|  | 编码器 A / B | **PC6 / PC7** | TIM8_CH1 / CH2 | 高级定时器 TIM8，编码器模式 |
|  | 方向控制 A / B | **PA4 / PA5** | GPIO Output | 电机 2 方向 |
| 电机 3（520 编码电机） | PWM | **PB0** | TIM3_CH3 | 调速 PWM（TIM3 共用） |
|  | 编码器 A / B | **PD12 / PD13** | TIM4_CH1 / CH2 | 通用定时器 TIM4，编码器模式 |
|  | 方向控制 A / B | **PD14 / PD15** | GPIO Output | 电机 3 方向 |
| CAN ↔ 上位机（USB-CAN 模块 → K1 Mini） | CAN_RX | **PD0** | CAN1_RX（AF9） | 经收发器接 USB-CAN 模块（USB → K1 Mini），500 kbps，CubeMX 已配置（v0.7.4） |
|  | CAN_TX | **PD1** | CAN1_TX（AF9） | 同上 |
| ICM20948 九轴 IMU | SCL | **PB8** | I2C1_SCL（AF4） | 硬件 I2C1 |
|  | SDA | **PB9** | I2C1_SDA（AF4） | 硬件 I2C1 |

变更记录：CAN 收发器由旧规划的 PB8/PB9 改为 **PD0/PD1**（CAN1 另一组映射）；IMU 定为 I2C1 **PB8/PB9**，二者正好错开。三路电机 PWM 共用 **TIM3**（CH1/CH2/CH3），编码器各自独立定时器。CAN 总线两端各接一个收发器模块（CAN_H/CAN_L 双绞线互联、共地，留 120 Ω 终端电阻），对端为 KICKPI K1 Mini（RK3568），v0.7.5 起经 **USB-CAN 模块** 以 SocketCAN `can0` 连接（撤销 SOC 板卡内置 CAN 控制器的设备树启用方案，40Pin 3/5 不再用于 CAN）。
