{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "ST-Link 调试 (openocd)",
            "type": "cortex-debug",
            "request": "launch",
            "servertype": "openocd",
            "cwd": "${workspaceFolder}",
            "executable": "${workspaceFolder}/build/OmniCar.elf",
            "device": "STM32F407VE",
            "configFiles": [
                "interface/stlink.cfg",
                "target/stm32f4x.cfg"
            ],
            "preLaunchTask": "build"
        }
    ]
}
